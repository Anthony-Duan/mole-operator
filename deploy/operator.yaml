apiVersion: apps/v1
kind: Deployment
metadata:
  name: mole-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: mole-operator
  template:
    metadata:
      labels:
        name: mole-operator
    spec:
      serviceAccountName: mole-operator
      containers:
        - name: mole-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - mole-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "mole-operator"
